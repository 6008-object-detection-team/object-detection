# The complete project snapshot includes builds, IDE files, logs and caches.
# Keep environments and credentials local if they are created later.
.venv/
venv/
env/
__pycache__/
.env
.env.*
!.env.example
.model_cache/

# Downloaded COCO subset for evaluate_detectors.py: regenerate with
# download_coco_subset.py rather than committing images/annotations.
/coco_eval/

# This 3.4 GB file is stored losslessly as adjacent Git LFS parts.
# Run python restore_large_files.py to restore its original name and contents.
/build/app/AIDetectionApp.pkg
*.restoring
